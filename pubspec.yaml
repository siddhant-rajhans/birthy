name: birthy
description: Never miss wishing a birthday again!
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  hive:
  hive_flutter:
  sqflite:
  flutter_local_notifications:
  cupertino_icons:

dev_dependencies:
  build_runner:
  hive_generator:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true